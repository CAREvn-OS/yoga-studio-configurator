// Supabase configuration
// These are safe to expose client-side — the bucket has public read access
// and RLS policies control write access via the anon key.
// Replace with your own Supabase project credentials.
export const SUPABASE_URL = ''
export const SUPABASE_ANON_KEY = ''
export const STORAGE_BUCKET = 'studio-media'
